<?php $this->headScript()
        ->appendFile($this->baseUrl('js/jquery.js', 'text/javascript'))
        ->appendFile($this->baseUrl('js/functions.js', 'text/javascript'))
        ->captureStart(); ?>


$(function(){

	var actionUrlProm='<?= $this->url(array('controller' => 'staff', 'action' => 'segnalaprom',), 'default');  ?>';
	var actionUrlAz='<?= $this->url(array('controller' => 'staff', 'action' => 'segnalaz',), 'default');  ?>';
        var formName='<?= $this->selezionaProm->getName() ?>';
                $('#selezCat').change(function (event) {
                   $('#selezAz').find('option').remove();
                   //$('#selezProm').append('<option>' +' -- Seleziona -- '+ '</option>');
                    $('#selezAz').append('<option>' +' -- Seleziona -- '+ '</option>');
                   setPromozioni(actionUrlProm, formName); 
                   
                });
                $('#selezProm').change(function (event) {
                   setAziende(actionUrlAz, formName); 
                    
                });
                
                
});         




<?php $this->headScript()->captureEnd() ?>

<div>
<h1 class="Titolo">Gestisci promozioni</h1>
<h3 class="Sottotitolo">Scegli la promozione che desideri;</h3>
<h3 class="Sottotitolo">Successivamente scegli se modificarla o cancellarla;</h3>
<br>
<br>

 <?php echo $this->selezionaProm ?>
 
</div>